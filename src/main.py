idea = """
    Dockerfile with a database (SQL).
    Dockerfile with a python image (Flask).
    Docker-compose to orchestrate.
    Only routes to return info based in data inside DB.
    User class - id, name, wallet_id, friends[]
    MiniVenmo class - id, user_id, balance, credit, credit_limit
"""